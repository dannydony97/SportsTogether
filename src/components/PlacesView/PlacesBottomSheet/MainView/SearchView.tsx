import React, {FC} from 'react';
import {Text, View} from 'react-native-ui-lib';

const SearchView: FC = () => {
  return (
    <View>
      <Text text10BL>SearchView: TODO!</Text>
    </View>
  );
};

export default SearchView;
